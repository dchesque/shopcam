# ========================================
# ShopFlow MVP - Configurações Simplificadas
# ========================================

# ============ SUPABASE ============
SUPABASE_URL=https://orzzycayjzgcuvcsrxsi.supabase.co
SUPABASE_ANON_KEY=seu_anon_key_aqui
SUPABASE_SERVICE_KEY=seu_service_key_aqui

# ============ CÂMERA RTSP ============
# URL da câmera Intelbras via RTSP
# Formato: rtsp://usuario:senha@ip:porta/cam/realmonitor?channel=1&subtype=0
# Exemplo Intelbras: rtsp://admin:senha123@192.168.1.100:554/cam/realmonitor?channel=1&subtype=0
CAMERA_RTSP_URL=rtsp://admin:senha@192.168.1.100:554/cam/realmonitor?channel=1&subtype=0

# FPS para processamento (5 FPS = bom equilíbrio entre performance e custo)
CAMERA_FPS_PROCESS=5

# Timeout para reconexão em segundos
CAMERA_RECONNECT_TIMEOUT=10

# ============ YOLO/IA ============
# Modelo YOLO (yolo11n.pt = mais leve, yolo11s.pt = mais preciso)
YOLO_MODEL=yolo11n.pt

# Confiança mínima para detecção (0.5 = 50%)
YOLO_CONFIDENCE=0.5

# Usar CPU ou GPU (cpu/cuda)
YOLO_DEVICE=cpu

# ============ DETECÇÃO DE GRUPOS ============
# Distância máxima em metros para considerar pessoas no mesmo grupo
GROUP_MAX_DISTANCE=1.5

# Tamanho mínimo de um grupo (2 = duas ou mais pessoas)
GROUP_MIN_SIZE=2

# ============ RECONHECIMENTO FACIAL ============
# Habilitar reconhecimento de funcionários (true/false)
FACE_RECOGNITION_ENABLED=true

# Tolerância do reconhecimento facial (0.6 = recomendado)
# Valores menores = mais restritivo, valores maiores = mais permissivo
FACE_TOLERANCE=0.6

# ============ API ============
API_HOST=0.0.0.0
API_PORT=8001
API_DEBUG=false

# ============ CORS (Frontend) ============
# Adicionar URL do frontend aqui
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://seu-frontend.vercel.app

# ============ STORAGE ============
UPLOAD_DIR=./uploads
SAVE_SNAPSHOTS=false  # Desabilitar snapshots para economizar espaço

# ============ LOGGING ============
LOG_LEVEL=INFO
LOG_FILE=logs/backend.log

# ============ OBSERVAÇÕES ============
# 1. Substituir CAMERA_RTSP_URL com URL rea22l da sua câmera
# 2. Preencher SUPABASE_SERVICE_KEY com a chave do Supabase
# 3. Se usar Tailscale, a URL será algo como: rtsp://admin:senha@100.64.1.2:554/...
# 4. Para testar localmente, usar IP local da câmera (192.168.x.x)
